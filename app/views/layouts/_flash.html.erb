<% flash.each do |type, message| %>
  <div class="alert <%= bootstrap_class_for(type) %> fade in">
    <div class="container">
      <button class="close" onclick="$(this).closest('.alert').fadeOut(500,function(){remove()});">×</button>
      <%= message %>
    </div>
  </div>
<% end %>